apiVersion: v1
kind: Service
metadata:
  name: web-wallet-service
spec:
  type: NodePort
  selector:
    app: web-wallet
  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 30030


#manifest file.